/**
 * Shared layout constants used across the application.
 * Keeps pixel values in sync between App.tsx, StatusBar, and other layout components.
 */
export const LAYOUT = {
  HEADER_HEIGHT: 40,      // px - workspace tabs header
  STATUS_HEIGHT: 24,      // px - status bar (compact)
} as const;
