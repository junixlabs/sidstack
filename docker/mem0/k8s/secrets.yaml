apiVersion: v1
kind: Secret
metadata:
  name: mem0-secrets
  namespace: sidstack
type: Opaque
stringData:
  # LiteLLM master key (change this!)
  LITELLM_MASTER_KEY: "sk-sidstack-prod-CHANGE-ME"

  # Provider API keys (add the ones you use)
  GOOGLE_API_KEY: ""
  # OPENAI_API_KEY: ""
  # ANTHROPIC_API_KEY: ""

  # Qdrant auth
  QDRANT_API_KEY: ""
